<%= form_with scope: @prefab_item, url: form_url, method: submit_method do |f| %>
  <%= labeled_text_field(f, :name).html_safe %>
  <%= check_boxes_for(@tags, :tag_ids, "Tags", f).html_safe %>
  <%= f.submit %>
<% end %>
