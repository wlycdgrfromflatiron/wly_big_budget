window.onload = () => {
    if (document.getElementsByTagName('body')[0].className === "frontPage"){
        // block form submission if password is missing
        // block form submission is email is missing
        // submit through AJAX
    }
}